services:
  api:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    ports:
      - "8000:8000"
    env_file: ../../.env
    environment:
      - ENV=production
      - API_RELOAD=false
    restart: unless-stopped
